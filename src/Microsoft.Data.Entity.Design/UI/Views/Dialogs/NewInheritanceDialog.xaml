<vs:DialogWindow x:Class="Microsoft.Data.Entity.Design.UI.Views.Dialogs.NewInheritanceDialog"
                 x:ClassModifier="internal"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:vs="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                 xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                 xmlns:dialogs="clr-namespace:Microsoft.Data.Entity.Design.UI.Views.Dialogs"
                 mc:Ignorable="d"
                 d:DesignWidth="420"
                 d:DesignHeight="250"
                 Width="420"
                 Height="250"
                 MinWidth="350"
                 MinHeight="220"
                 ShowInTaskbar="False"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 FontFamily="{DynamicResource VsFont.EnvironmentFontFamily}"
                 FontSize="{DynamicResource VsFont.EnvironmentFontSize}"
                 Background="{DynamicResource VsBrush.ToolWindowBackground}"
                 Title="{x:Static dialogs:DialogsResource.NewInheritanceDialog_Title}"
                 HasHelpButton="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DialogStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Info Text -->
        <TextBlock Grid.Row="0"
                   Text="{x:Static dialogs:DialogsResource.NewInheritanceDialog_InfoText}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <!-- Base Entity Label -->
        <TextBlock Grid.Row="1"
                   Text="{x:Static dialogs:DialogsResource.NewInheritanceDialog_SelectBaseEntity}"
                   Margin="0,0,0,4"/>

        <!-- Base Entity ComboBox -->
        <ComboBox x:Name="BaseEntityComboBox"
                  Grid.Row="2"
                  Margin="0,0,0,16"
                  SelectionChanged="BaseEntityComboBox_SelectionChanged"/>

        <!-- Derived Entity Label -->
        <TextBlock Grid.Row="3"
                   Text="{x:Static dialogs:DialogsResource.NewInheritanceDialog_SelectDerivedEntity}"
                   Margin="0,0,0,4"/>

        <!-- Derived Entity ComboBox -->
        <ComboBox x:Name="DerivedEntityComboBox"
                  Grid.Row="4"
                  Margin="0,0,0,16"/>

        <!-- Button Panel -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="{x:Static dialogs:DialogsResource.OKButton_Text}"
                    MinWidth="75"
                    Height="23"
                    Margin="0,0,8,0"
                    Click="OkButton_Click"
                    IsDefault="True"
                    IsEnabled="False"
                    AutomationProperties.Name="{x:Static dialogs:DialogsResource.OKButton_AccessibleName}"/>
            <Button Content="{x:Static dialogs:DialogsResource.CancelButton_Text}"
                    MinWidth="75"
                    Height="23"
                    IsCancel="True"
                    AutomationProperties.Name="{x:Static dialogs:DialogsResource.CancelButton_AccessibleName}"/>
        </StackPanel>
    </Grid>
</vs:DialogWindow>
